{
  "name": "casey-shiray-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/quarantine-oversize-assets.mjs",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^13.5.4",
    "@astrojs/mdx": "^5.0.6",
    "astro": "^6.3.7",
    "gsap": "^3.15.0",
    "lenis": "^1.3.23",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.7",
    "wrangler": "^4.94.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
